# Match failed requests (4xx) to auth endpoints
[Definition]
failregex = ^<HOST> .+ "(GET|POST|PUT|PATCH|DELETE) /api/(login|register|forgot-password|reset-password).+" (400|401|403|422|429) .+$
ignoreregex =
