# Match clear abuse signals: repeated 400/403/405/408 errors
# Excludes 404 (broken links, missing assets) and 429 (already rate-limited)
[Definition]
failregex = ^<HOST> .+ "(GET|POST|PUT|PATCH|DELETE|HEAD|OPTIONS) .+" (400|403|405|408) .+$
ignoreregex =
